## How to use these:
- Fill the Values some like RoleId on welcome-message are not needed.
- rename the folder from `configs.example` -> `configs` and on the next start you can test with `/test join | leave`